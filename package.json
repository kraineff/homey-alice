{
  "name": "homey-alice",
  "module": "src/index.js",
  "scripts": {
    "build": "bun build src/index.ts --target=bun --outfile=server.js --minify",
    "start": "bun run build && pm2 delete homey && pm2 start \"bun src/index.ts\" --name \"homey\"",
    "restart": "bun run build && pm2 restart homey"
  },
  "dependencies": {
    "elysia": "^1.0.20",
    "homey-api": "^3.4.30",
    "jose": "^5.3.0"
  },
  "devDependencies": {
    "bun-types": "^1.1.8"
  }
}